name: Deploy Telegram Bot

on:
  push:
    branches: [ main, master ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to VPS
        uses: appleboy/ssh-action@v0.1.5
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e  # Останавливать скрипт при любой ошибке
            
            echo "🚀 Начинаем деплой Telegram бота..."
            
            # Переход в директорию проекта
            PROJECT_PATH="${{ secrets.PROJECT_PATH }}"
            echo "📁 Рабочая директория: $PROJECT_PATH"
            mkdir -p $PROJECT_PATH
            cd $PROJECT_PATH

            # Сохранение .env файла
            echo "🔧 Создание .env файла..."
            echo "BOT_API_TOKEN=${{ secrets.BOT_API_TOKEN }}" > .env
            echo "LOG_LEVEL=INFO" >> .env
            echo "✅ .env файл создан"

            # Клонирование или обновление кода
            echo "📦 Обновление кода из репозитория..."
            if [ -d .git ]; then
              git pull origin main
              echo "✅ Код обновлен через git pull"
            else
              git clone https://github.com/${{ github.repository }} .
              echo "✅ Репозиторий склонирован"
            fi

            # Сборка Docker образа
            echo "🏗️ Сборка Docker образа..."
            docker build -t telegram-bot:latest .
            echo "✅ Docker образ собран"

            # Остановка и удаление старого контейнера
            echo "🛑 Остановка старого контейнера..."
            docker stop telegram-bot-container 2>/dev/null || true
            docker rm telegram-bot-container 2>/dev/null || true
            echo "✅ Старый контейнер удален"

            # Запуск нового контейнера
            echo "🚀 Запуск нового контейнера..."
            docker run -d \
              --name telegram-bot-container \
              --restart unless-stopped \
              --env-file .env \
              telegram-bot:latest
            
            echo "✅ Контейнер запущен, начинаем проверку..."

            # ========== ПРОВЕРКА СТАБИЛЬНОСТИ ==========
            echo "🔍 Проверка стабильности работы бота..."
            
            # Даем время на инициализацию и возможные перезапуски
            echo "⏳ Ожидание 15 секунд для инициализации..."
            sleep 15

            # Шаг 1: Проверяем, работает ли контейнер сейчас
            if [ ! "$(docker ps -q -f name=telegram-bot-container)" ]; then
              echo "❌ ОШИБКА: Контейнер не работает!"
              echo ""
              echo "📋 Статус контейнера:"
              docker ps -a -f name=telegram-bot-container
              echo ""
              echo "📋 Логи контейнера:"
              docker logs telegram-bot-container
              echo ""
              echo "📋 Содержимое .env (токен скрыт):"
              cat .env | sed 's/BOT_API_TOKEN=.*/BOT_API_TOKEN=***/'
              exit 1
            fi
            echo "✅ Контейнер работает"

            # Шаг 2: Проверяем количество перезапусков
            RESTART_COUNT=$(docker inspect telegram-bot-container --format='{{.RestartCount}}')
            if [ "$RESTART_COUNT" -gt 0 ]; then
              echo "⚠️ Контейнер перезапускался $RESTART_COUNT раз(а)"
              if [ "$RESTART_COUNT" -gt 2 ]; then
                echo "❌ ОШИБКА: Слишком много перезапусков ($RESTART_COUNT)"
                echo ""
                echo "📋 Логи контейнера:"
                docker logs telegram-bot-container --tail 50
                exit 1
              fi
            else
              echo "✅ Перезапусков не было"
            fi

            # Шаг 3: Проверяем логи на наличие критических ошибок
            echo "📋 Анализ логов на наличие ошибок..."
            
            # Проверяем конкретную ошибку с токеном
            if docker logs telegram-bot-container 2>&1 | grep -q "BOT_API_TOKEN is not defined"; then
              echo "❌ КРИТИЧЕСКАЯ ОШИБКА: BOT_API_TOKEN не передан в контейнер!"
              echo ""
              echo "📋 Логи:"
              docker logs telegram-bot-container --tail 20
              echo ""
              echo "📋 Содержимое .env (токен скрыт):"
              cat .env | sed 's/BOT_API_TOKEN=.*/BOT_API_TOKEN=***/'
              exit 1
            fi
            
            # Проверяем другие ошибки
            ERROR_LOGS=$(docker logs telegram-bot-container 2>&1 | grep -i "error\|exception\|fail" | head -20)
            if [ ! -z "$ERROR_LOGS" ]; then
              echo "⚠️ Найдены ошибки в логах (но контейнер работает):"
              echo "$ERROR_LOGS"
            else
              echo "✅ Ошибок в логах не обнаружено"
            fi

            # Шаг 4: Проверяем, что процесс Node.js жив
            if docker exec telegram-bot-container ps aux 2>/dev/null | grep -q node; then
              echo "✅ Node.js процесс работает"
            else
              echo "❌ ОШИБКА: Node.js процесс не найден внутри контейнера!"
              echo ""
              echo "📋 Процессы в контейнере:"
              docker exec telegram-bot-container ps aux 2>/dev/null || echo "Не удалось выполнить ps aux"
              echo ""
              echo "📋 Логи контейнера:"
              docker logs telegram-bot-container --tail 30
              exit 1
            fi

            # Шаг 5: Проверяем, что переменная окружения доступна внутри контейнера
            if docker exec telegram-bot-container env | grep -q "BOT_API_TOKEN"; then
              echo "✅ Переменная BOT_API_TOKEN доступна в контейнере"
            else
              echo "❌ ОШИБКА: Переменная BOT_API_TOKEN не найдена в окружении контейнера!"
              echo ""
              echo "📋 Переменные окружения в контейнере:"
              docker exec telegram-bot-container env | grep -E "BOT|NODE" || echo "Переменные не найдены"
              exit 1
            fi

            # Финальная проверка: даем еще немного времени и убеждаемся, что контейнер не упал
            echo "⏳ Финальная проверка стабильности (5 секунд)..."
            sleep 5
            
            if [ ! "$(docker ps -q -f name=telegram-bot-container)" ]; then
              echo "❌ ОШИБКА: Контейнер упал после проверок!"
              echo ""
              echo "📋 Последние логи:"
              docker logs telegram-bot-container --tail 50
              exit 1
            fi

            # ========== УСПЕШНОЕ ЗАВЕРШЕНИЕ ==========
            echo ""
            echo "🎉🎉🎉 ДЕПЛОЙ УСПЕШНО ЗАВЕРШЕН! 🎉🎉🎉"
            echo ""
            echo "📊 Итоговая информация:"
            echo "   • Контейнер: telegram-bot-container"
            echo "   • Статус: $(docker inspect telegram-bot-container --format='{{.State.Status}}')"
            echo "   • Перезапусков: $(docker inspect telegram-bot-container --format='{{.RestartCount}}')"
            echo "   • Время работы: $(docker inspect telegram-bot-container --format='{{.State.StartedAt}}' | cut -d'T' -f2 | cut -d'.' -f1)"
            echo ""
            echo "📋 Последние строки лога:"
            docker logs --tail 5 telegram-bot-container
            echo ""
            echo "✅ Бот успешно запущен и работает стабильно!"

            # Очистка старых образов
            echo "🧹 Очистка старых образов..."
            docker image prune -f
            echo "✅ Очистка завершена"